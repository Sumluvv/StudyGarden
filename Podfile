# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'
# For macOS support, uncomment the next line and configure a macOS target in your Xcode project/workspace
# platform :osx, '11.0'

target 'StudyGarden' do
  project 'StudyGarden.xcodeproj'
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StudyGarden (limit to supported ones)
  pod 'FirebaseCore'
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'

end

